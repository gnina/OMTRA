# Core worker dependencies
redis==5.0.1
rq==1.15.1
python-dotenv==1.0.0
pyyaml==6.0.1
pandas>=1.5.0
requests>=2.28.0
aiofiles>=23.0.0
pydantic>=2.0.0
natsort>=8.0.0

# OMTRA model dependencies (installed separately in Docker)
# torch, torch-geometric, rdkit, etc. are installed via the main OMTRA environment
