_target_: omtra.dataset.data_module.MultiTaskDataModule

# Core parameters
tasks: ${tasks}
multitask_dataset_config: ${tasks} #TODO: bundle individual configs for each task into a single config file
prior_config: None #TODO: clarify functionality
batch_size: ${batch_size}
num_workers: ${num_workers}
